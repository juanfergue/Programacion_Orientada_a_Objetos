package ejclase;

import java.util.*;

public class ejercicio {

	public static void main(String[] args) {
		Map<String, Integer> c = new TreeMap<>();
		/*		for(String pal : args) {
			if(!c.containsKey(pal)) { c.containsKey() comprueba si en el Map que estamos analizando que esta la palabra que buscamos.
				c.put(pal, 1);
				
			}else {
				c.put(pal, c.get(pal)+1);
			}
			
		}
		System.out.println(c);
		Otra forma de hacer el ejercicio
		for(String pal : args) {
			Integer cnt = c.get(pal);
			if(cnt == null) {
				c.put(pal, 1);
			}else {
				c.put(pal, cnt + 1);
			}
			println(c);
			
		
		*/
		
		
		}
	}

}
